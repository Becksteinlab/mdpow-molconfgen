name: mdpow-molconfgen-test
channels:
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip

  # MDAnalysis
  - MDAnalysis


  - gromacswrapper

  # for energy evaluation in the tests (will not work on Windows)
  # (install your own version of GROMACS for production use)
  - gromacs  # [not win]

  # other package dependencies
  - rdkit
  - numpy
  - tqdm

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov

 

